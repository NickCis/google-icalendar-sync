{
  "name": "icalendar-sync",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "deploy": "yarn now && yarn now alias",
    "prettier": "prettier --trailing-comma es5 --single-quote --write --write '{api,www}/**/*.js'",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^24.5.0",
    "now": "^14.0.3",
    "prettier": "^1.16.4"
  },
  "dependencies": {
    "@nickcis/ical-generator": "1.6.0-20190322194000",
    "compose-function": "^3.0.3",
    "googleapis": "^39.1.0",
    "micro": "^9.3.3",
    "micro-morgan": "^0.1.0",
    "micro-query": "^0.3.0",
    "micronize": "^0.1.0"
  }
}
