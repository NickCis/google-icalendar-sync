{
  "name": "icalendar-sync",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "deploy": "yarn now && yarn now alias"
  },
  "devDependencies": {
    "now": "^14.0.3"
  },
  "dependencies": {
    "googleapis": "^38.0.0",
    "ical-generator": "^1.6.0",
    "micro": "^9.3.3",
    "micro-query": "^0.3.0"
  }
}
